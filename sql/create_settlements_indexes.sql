create index settlements_id
    on settlements (id);

create index settlements_fias_id
    on settlements (fias_id);

create index settlements_kladr_id
    on settlements (kladr_id);

create index settlements_type_short
    on settlements (type_short);